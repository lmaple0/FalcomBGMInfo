name: Build FalcomModernBGMInfo DLL

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, x86]
        
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup MSVC
      uses: microsoft/setup-msbuild@v1.1
      
    - name: Setup vcpkg
      uses: lukka/run-vcpkg@v11
      with:
        vcpkgGitCommitId: 'a42af01b72c28a8e1d7b48107b33e4f286a55ef6'
        
    - name: Install vcpkg dependencies (x64)
      if: matrix.arch == 'x64'
      run: |
        vcpkg install yaml-cpp:x64-windows-static minhook:x64-windows-static directx-headers:x64-windows-static directxtk:x64-windows-static
        
    - name: Install vcpkg dependencies (x86)
      if: matrix.arch == 'x86'
      run: |
        vcpkg install yaml-cpp:x86-windows-static minhook:x86-windows-static directx-headers:x86-windows-static directxtk:x86-windows-static
        
    - name: Configure CMake (x64)
      if: matrix.arch == 'x64'
      run: |
        cmake -B build -S src/FalcomModernBGMInfo -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake" -A x64
        
    - name: Configure CMake (x86)
      if: matrix.arch == 'x86'
      run: |
        cmake -B build -S src/FalcomModernBGMInfo -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake" -A Win32
        
    - name: Build
      run: |
        cmake --build build --config Release
        
    - name: Display build output
      run: |
        dir build\Release\*.dll
        
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: FalcomModernBGMInfo-${{ matrix.arch }}-Release
        path: build/Release/FalcomModernBGMInfo.dll
        retention-days: 30
